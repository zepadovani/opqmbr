# panoramapqmus — espelho de deploy (GitHub Pages)

Gerado automaticamente por `mirrar_site_github.sh`, do repositório Forgejo
[baixarsucupira](https://git.pdvn.cc/padovani/baixarsucupira) — commit
`06ce7d9a` (árvore de trabalho com 9 alterações não commitadas).

**Não edite aqui.** Este repositório não tem histórico próprio — cada publicação
sobrescreve o anterior por inteiro. Mudanças de código entram no repositório
Forgejo; mudanças de dados entram publicando uma nova release lá
(`publicar_base.sh`). O workflow em `.github/workflows/deploy.yml` builda o
site a partir dessa release a cada push aqui.

Ver `docs/DEPLOY.md` (caminho C) no repositório de origem para o resto.
